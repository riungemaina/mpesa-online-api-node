# A js file secrets.js takes in the following

should be created in this directory

```javascript
module.exports = {
    // application pass key given by safaricom (string)
    passKey: "",

    // recieving till number (number)
    till: ,

    // the call back url from ngrok/localtunnel (string)
    ngrokCallUrl: "",
};
```
